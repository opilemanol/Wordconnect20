name: Build Signed Release AAB

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      STORE_PASSWORD: ${{ secrets.STORE_PASSWORD || 'android123' }}
      KEY_PASSWORD: ${{ secrets.KEY_PASSWORD || 'android123' }}
      KEY_ALIAS: ${{ secrets.KEY_ALIAS || 'upload' }}
      KEYSTORE_PATH: ${{ github.workspace }}/my-upload-keywc.jks

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 1. Decode keystore file FIRST before initializing Gradle
      - name: Decode Saved Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > "$KEYSTORE_PATH"

      # 2. Setup Gradle after keystore is placed
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: false

      - name: Build Signed Release AAB
        run: gradle bundleRelease -Djava.security.properties=/dev/null --stacktrace

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: app/build/outputs/bundle/release/app-release.aab
